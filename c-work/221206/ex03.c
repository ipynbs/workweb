#include <stdio.h>

int main(){

    // 10 + 20 해서 변수 num에 넣어서 출력하기
    int a = 10;
    int b = 20;
    int num = a + b;
    printf("num = %d",num);

    return 0;
}
// alt + shift + f = 자동정령 (약간의 팁)
